Upstream-Status: Pending
Exit: Drop when GNOME Shell stops shelling out to unzip for extension extraction
 (GNOME/gnome-shell#8567, GNOME/gnome-shell!4403), or when the image ships a
 traditional Info-ZIP unzip again.
Subject: gnome-shell: use portable unzip flags for extension extraction

diff --git a/elements/core/gnome-shell.bst b/elements/core/gnome-shell.bst
index ebce5ab..4a81cb9 100644
--- a/elements/core/gnome-shell.bst
+++ b/elements/core/gnome-shell.bst
@@ -12,6 +12,8 @@ sources:
   url: gnome:libshew.git
   directory: subprojects/libshew
   ref: d16afc40412b565d2bbecf80335f54a19a978009
+- kind: patch
+  path: patches/gnome-shell/0001-extensionDownloader-use-portable-unzip-flags.patch
 
 build-depends:
 - (@): include/gcc-for-recc.yml
diff --git a/patches/gnome-shell/0001-extensionDownloader-use-portable-unzip-flags.patch b/patches/gnome-shell/0001-extensionDownloader-use-portable-unzip-flags.patch
new file mode 100644
index 0000000..1f8b144
--- /dev/null
+++ b/patches/gnome-shell/0001-extensionDownloader-use-portable-unzip-flags.patch
@@ -0,0 +1,18 @@
+Upstream-Status: Pending
+Exit: Drop when GNOME Shell stops shelling out to unzip for extension extraction
+ (GNOME/gnome-shell#8567, GNOME/gnome-shell!4403), or when the image ships a
+ traditional Info-ZIP unzip again.
+
+diff --git a/js/ui/extensionDownloader.js b/js/ui/extensionDownloader.js
+index 16f765a..191da18 100644
+--- a/js/ui/extensionDownloader.js
++++ b/js/ui/extensionDownloader.js
+@@ -124,7 +124,7 @@ async function extractExtensionArchive(bytes, dir) {
+     stream.close_async(GLib.PRIORITY_DEFAULT, null);
+ 
+     const unzip = Gio.Subprocess.new(
+-        ['unzip', '-uod', dir.get_path(), '--', file.get_path()],
++        ['unzip', '-od', dir.get_path(), '--', file.get_path()],
+         Gio.SubprocessFlags.NONE);
+     await unzip.wait_check_async(null);
+ 
